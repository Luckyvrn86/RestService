package org.ssemchenko.restservice.servlet.mapper.impl;

import org.ssemchenko.restservice.model.Student;
import org.ssemchenko.restservice.servlet.dto.IncomingDto;
import org.ssemchenko.restservice.servlet.dto.OutGoingDto;
import org.ssemchenko.restservice.servlet.mapper.StudentDtomapper;

public class StudentDtomapperImpl implements StudentDtomapper {
    @Override
    public Student map(IncomingDto incomingDto) {
        return null;
    }

    @Override
    public OutGoingDto map(Student student) {
        return null;
    }
}
